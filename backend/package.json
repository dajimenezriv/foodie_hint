{
  "name": "backend",
  "module": "index.ts",
  "type": "module",
  "private": true,
  "scripts": {
    "========== Start ==========": "",
    "start": "bun index.ts",
    "========== Testing ==========": "",
    "vitest": "vitest",
    "vitest:once": "vitest run",
    "vitest:one": "vitest run related x",
    "vitest:cov": "vitest run --coverage"
  },
  "devDependencies": {
    "@vitest/coverage-v8": "^3.1.4",
    "prisma": "^6.8.2",
    "vite-tsconfig-paths": "^5.1.4",
    "vitest": "^3.1.4"
  },
  "peerDependencies": {
    "typescript": "^5"
  },
  "dependencies": {
    "@prisma/client": "^6.8.2",
    "zod": "^3.25.20"
  }
}
